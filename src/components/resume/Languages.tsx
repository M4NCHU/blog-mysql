import { FC } from "react";
import ResumeHeader from "./ResumeHeader";

interface LanguagesProps {}

const Languages: FC<LanguagesProps> = ({}) => {
  return (
    <>
      <ResumeHeader title="Languages" id="languages" />
    </>
  );
};

export default Languages;
